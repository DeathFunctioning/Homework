#include <push_swap.h>

int	main(int argc, char **argv)
{

	if (argc > 1)
	{
		while (argv[i])
		{
			stack_a = create_stack(ft_atoi(argv[i]));


		}
		return (0);
	}
		write(1, "Error\n", 6);
		return (1);
}

